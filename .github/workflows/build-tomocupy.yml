name: Build TomoCuPy Container

on:
  push:
    paths:
      - 'recipes/tomocupy-beta.recipe'
      - '.github/workflows/build-tomocupy.yml'
  pull_request:
    paths:
      - 'recipes/tomocupy-beta.recipe'
      - '.github/workflows/build-tomocupy.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Build container
        run: |
          podman build -f recipes/tomocupy-beta.recipe -t tomocupy:beta

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push container
        run: |
          podman tag tomocupy:beta ghcr.io/globus-gladier/aps_recipes/tomocupy:beta
          podman push ghcr.io/globus-gladier/aps_recipes/tomocupy:beta 