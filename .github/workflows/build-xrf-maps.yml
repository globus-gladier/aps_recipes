name: Build XRF Maps Container

on:
  push:
    paths:
      - 'recipes/xrf_maps-beta.recipe'
      - '.github/workflows/build-xrf-maps.yml'
  pull_request:
    paths:
      - 'recipes/xrf_maps-beta.recipe'
      - '.github/workflows/build-xrf-maps.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Build container
        run: |
          podman build -f recipes/xrf_maps-beta.recipe -t xrf-maps:beta

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push container
        run: |
          podman tag xrf-maps:beta ghcr.io/globus-gladier/aps_recipes/xrf-maps:beta
          podman push ghcr.io/globus-gladier/aps_recipes/xrf-maps:beta 